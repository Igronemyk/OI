#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int n,m,a;
    long long d;
    cin >> n >> m >> a >> d;
    long long nowTime = 0,result = 0;
    for(int i = 0;i < m;i++) {
        long long tmpTime;
        cin >> tmpTime;
        if(tmpTime / a > nowTime) {

        }
    }
    return 0;
}
