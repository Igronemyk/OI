#include <iostream>
#include <cstring>
#include <algorithm>

using namespace std;

int mem[26][1010],len[26];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    memset(len,-1,sizeof(len));
    string A,B;
    cin >> A >> B;

    return 0;
}
