#include <cstdio>
#include <algorithm>
#include <iostream>
#include <cstring>

using namespace std;


char b[10000000];

int main(){
    fill(b,b + 10000000,0);
    //memset(b,0,sizeof(b));
    return 0;
}
